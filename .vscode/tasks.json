{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "br_fontTest",
            "type": "shell",
            "command": "cd build && make && cp tests/Rfonts/fontTest ../tests/Rfonts/ && cd ../tests/Rfonts/ &&  ./fontTest",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}